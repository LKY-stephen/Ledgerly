# Data Scheme Design Summary v4

Legacy note:

- `v4` is no longer the active baseline.
- The current version-managed design-summary entrypoint is `prd/database-schema-summary/README.md`.
- The active sparse-input baseline is `prd/database-schema-summary/v1.md`.
- The implemented runtime contract now follows the hybrid `v1` shape documented in `docs/contracts/local-storage.md`.

This file remains only as historical lineage for older design discussions. Do not treat it as the active schema contract or the current implementation target.
